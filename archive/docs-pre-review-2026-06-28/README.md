# Archived docs tree before review

This directory preserves documentation that used to live under `docs/` before the
2026-06-28 review cleanup.

The active `docs/` tree is intentionally reduced to `docs/agent-workflow/` plus the Makefile-referenced LTP blacklist input directory. Other historical docs and guard-script fixtures were moved here, and live script references were updated together.

Moved from `docs/`:

- `README.md`
- `archive/`
- `developer-guide/`
- `ltp-real-semantics-repair-2026-06-07/`

Kept under `docs/` because it is a live Makefile input:

- `docs/ltp-full-sweep-blacklist-2026-05-30-arch/`
