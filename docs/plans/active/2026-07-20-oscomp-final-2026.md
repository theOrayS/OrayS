# OS competition final-2026 持续提分计划

状态：`IN_PROGRESS`

开始日期：2026-07-20

执行分支：`dev/oscomp-final-2026`

不可回退基线：`d9891d0254b5663436ef53893c105138fc7f009f`

外部只读协议：`/root/testsuits-for-oskernel-final-2026` 的
`final-2026@15e0355bbee0373de4048002448cee37dbb7ca1b`

## 1. 目标与验收

- 在 RISC-V64 与 LoongArch64 guest 内真实执行 final-2026 CAgent 与 BuildStorm，持续提高
  judge 可复算的真实得分。
- 每个生产修复遵循通用 RED -> GREEN：先证明 Linux/POSIX/ABI 缺陷，再做最小修复，
  再跑双架构定向测试和相称的 canonical profile。
- CAgent 必须完整记录 10 个身份、pass/reject、真实 elapsed_ms 与 judge 分数；BuildStorm
  必须区分 toolchain、minibuild、compile correctness 和真实 `/proc/uptime` elapsed_s。
- 终态要求同一 clean 候选上两架构全部适用分数满分、quick/baseline/full-all 完整通过、
  无隐藏失败/超时/阻塞，并完成独立只读 review 与文档闭环。

## 2. 非目标与诚信边界

- 不修改外部仓库、judge、脚本、官方镜像或评分公式；不下载非官方镜像冒充官方输入。
- 不按测例名、prompt、路径、固定答案、输出、执行顺序或架构特化生产实现。
- 不伪造 syscall、版本、CPU、网络、文件、时间、`/proc/uptime`、退出码或结果 marker。
- 不把旧 24-group 初赛镜像、host 代跑、缺镜像、空套件、超时或 parser 异常计为得分。
- 不操作其他工作树，不 push、不创建 PR、不触发 GitHub Actions，除非后续明确授权。

## 3. 已确认基线与外部缺口

- 当前分支/HEAD 精确为合同值，任务开始时 worktree clean；本地基线分支指向同一提交。
- 外部 final-2026 checkout clean，精确 SHA 与合同一致。
- 现有 `/root/sdcard-rv.img` 与 `/root/sdcard-la.img` 是 2025-06 的 4 GiB 初赛镜像，SHA-256
  分别为 `43364754...cb99` 与 `1aa79d03...b50`，根目录只有 `/musl`、`/glibc` 和
  `lost+found`；它们不是 README 描述的 Debian + Rust toolchain + tgoskits final rootfs。
- QEMU RV/LA 与 qemu-img 均为 9.2.4；宿主只有 2 个物理/在线 CPU、3.6 GiB RAM、无 swap，
  根盘剩余约 7.2 GiB。BuildStorm 官方合同要求 8 个物理核、guest 8 GiB，当前宿主不足。
- canonical manifest 只有旧 `official` 24-group profile；59 个注册 case 中没有
  final-2026/CAgent/BuildStorm case。必须新增 fail-closed、版本化的 final 协议入口，
  但缺官方镜像时仍只能得到 `BLOCKED`，不能产生分数。

## 4. 阶段

### Phase 0 - 可信基线与协议建模

- [x] 完整阅读仓库合同、测试入口、相关 CI/计划/日志与外部 README/scripts/judge/config/Makefile。
- [x] 固定 Git、外部 SHA、工具、镜像哈希、CPU/内存/磁盘事实。
- [x] 列出现有 canonical suite，证明 final-2026 尚未注册。
- [x] 只读核验旧镜像不含 final payload，并形成 RV/LA x CAgent/BuildStorm 的明确 `BLOCKED` 基线矩阵。

### Phase 1 - final-2026 fail-closed runner

- [x] 增加版本化 final 结果模型，精确绑定外部 commit、两架构、CAgent 10 identity
  与 BuildStorm 3 个脚本化判定项；缺失/重复/格式错误/非零退出/超时必须非 PASS。
- [x] 增加只读 image provenance、qcow2 overlay、8c/8G QEMU、进程组超时和证据捕获；
  manifest 暂无官方 image 条目，因此任何本地文件都不能越过 provenance 检查。
- [x] 增加 parser 正负 mutation tests，不复制或修改 judge 逻辑来放宽结论。
- [x] 将 final 结果模型接入 runner contract，并增加 runner mutation tests；canonical 四 case
  与独立 `final-2026` profile 已注册。
- [x] 保持旧 `official` profile 不变；按根仓库合同“full 包含全部已注册适用测试”的要求，
  `full` 现在同时包含旧 official 与 final 四 case，缺任一正式 artifact 时明确非 PASS。

### Phase 2 - 首个真实 RED -> GREEN

- [x] 官方 final 镜像可用时先跑未修改 d989 基线；不可用时用不冒充官方的最小通用 guest
  行为回归定位动态 ELF、procfs、并发、文件系统或工具链首个真实根因。
- [x] 先提交 test-only red，再最小修复生产实现，跑 RV/LA targeted green；首轮完成动态
  `/proc/uptime`、`/dev/null` open-file access-mode 与真实 per-task CPU affinity 修复，独立
  review 留到候选批次复核。
- [ ] 有明确预期 delta 时才重跑对应 final 官方 case，保留首次失败和重试。

### Phase 3 - 持续提分与性能

- [ ] 逐一清除 CAgent 真实 reject/慢项，不能硬编码 prompt 或答案；当前已补齐其真实调用的
  BusyBox `df`/`nproc` PATH 入口，并以双架构 guest 证明并发 TCP/fork 与
  pipe/fork/execve/wait4 内核链路，正式 rootfs 到位后仍须用原脚本取得真实结果。
- [ ] BuildStorm 依次打通 toolchain、minibuild、full compile；8-vCPU 启动和 `nproc` 所依赖的
  affinity 可见性已由双架构 canonical semantic smoke 证明；版本化 `clone3` 基础
  process ABI 已从双架构 syscall 435 RED 闭环到 clean GREEN，Cargo/glibc 的 exact worker
  thread flags、TLS、显式栈、parent/child TID、`clear_child_tid` futex join 与真实 child 输出
  已双架构 clean GREEN；`posix_spawn` 的 `CLONE_VM|CLONE_VFORK`、显式栈、父进程挂起至
  exec/exit 的形态也已双架构 clean GREEN。真实 Cargo host trace 命中的
  `MADV_DONTNEED` 已从逐字节清零、保留物理页改为按 VMA 批量撤销 PTE/释放 frame，
  双架构 clean guest 门禁同时证明归零、refault、fork/COW 隔离，测得相同 128 MiB 工作负载
  的 syscall 时间分别下降 57.4%/63.7%。`statx` host trace 的现有参数形态已经受支持；进一步
  审计发现真正的 BuildStorm correctness 阻塞是 ext4 根驱动全局只读，无法在 `/work` 或任何
  lower 目录创建构建产物。现已用通用 ext4 lower + volatile ramfs upper 替代只读根挂载，
  支持 copy-up、whiteout、merged readdir、truncate、权限、删除与跨目录 rename，官方镜像
  backing 仍保持只读。随后又以通用 POSIX overlay 合同闭环打开句柄生命周期：同一路径的
  活跃句柄共享 backing，源句柄跨 rename/unlink 后继续读写，被覆盖目标的旧句柄与新 namespace
  对象保持独立，lower 文件在 unlink 前按需 copy-up，父目录 rename 会同步迁移活跃子目录句柄。
  `statfs` 的 guest ABI 已由用户态层按挂载类型提供，并非 ext4 VFS 缺口；随后针对真实 Cargo
  文件访问形态发现 lower ext4 符号链接被普通 metadata 跟随且 `readlinkat` 返回 `EINVAL`。
  VFS 现已通用暴露 no-follow metadata 与 link target，overlay 保持委托，`lstat`/`statx`
  no-follow/readlink 已在双架构 fresh ext4 guest 中验证；readlink copyout 同时收敛到 typed
  `UserSlice` 边界，旧式调用库存从 47 降到 45。随后又修复了 final suite 启动链路：adapter
  从 provenance 绑定的 BuildStorm guest script 派生 `/scripts`，为脚本 14400 s 内部 timeout
  提供 18000 s guest budget；autorunner 现区分普通 POSIX `/bin/sh` 与 BusyBox provider、保留
  配置 suite 的 judge group label，并接受合法的多控制流 END frame。clean `b5616bbd` quick
  47/47、RV/LA runtime 各 1/1 PASS；源码与双架构构建证据表明启动链路不再在发现、解释器、
  label 或 60 s timeout 层静态阻止真实 Debian userspace。随后用仅含标准 `/bin`、`/lib*`
  的 fresh ext4 探针复现 LA64 动态 glibc 程序找不到 `libc.so.6`，而 RV64 同载荷成功；
  test-only `007ae7aa` 将 root runtime 与 SONAME 搜索契约固化，`adacda8c` 取消按 loader
  basename 把 rootfs ELF 重映射到 `/glibc`，并让标准 `/lib*` 先于兼容根参与裸 SONAME
  查询。相同 LA64 镜像由退出 127 转为退出 0，RV64 保持退出 0；`66a453fe` 增加 mutation
  保护。该证据消除了一个 BuildStorm 动态工具链前置阻塞，但正式执行仍等待官方 rootfs。
  随后通用 BusyBox fork/`linkat` 探针在两架构复现：`ln` 子进程返回成功，但父脚本读取
  新别名得到 `ENOENT`。根因是 fork 深拷贝全部路径元数据表，使文件系统 namespace 状态
  退化成进程私有。test-only `ab2d9880` 固化共享 namespace 契约，`1b5aa307` 将权限、inode、
  symlink、hardlink、xattr、时间等 11 类路径元数据改为 fork 共享的 `Arc<Mutex<...>>`；同一
  fresh ext4 载荷在 RV/LA 均由 outer FAIL 转为 PASS。clean `1b5aa307` quick 47/47、两架构
  runtime 各 1/1 PASS。该修复覆盖 Cargo 常见的跨进程文件操作，但正式 toolchain 仍未运行；
  随后的生命周期审计又证明 canonical 名仍被当作唯一物理 backing：删除它会让剩余别名
  `ENOENT`，重命名它也不会迁移别名映射。test-only `74b1a4e1` 固化 canonical promotion 与
  rename-following 契约；`ef3d6ef9` 在 unlink 时把物理目录项原子提升到一个剩余别名，并让
  普通 rename/rename-exchange 同步整组 canonical 映射与链接计数。双架构 fresh ext4 guest
  均验证 canonical rename 后旧别名与新名称可读、canonical unlink 后剩余别名可读；clean
  `ef3d6ef9` quick 47/47、两架构 runtime 各 1/1 PASS。继续按 Cargo 的通用系统调用形态审计时，
  又发现 `flock(LOCK_EX)` 在竞争下无论是否携带 `LOCK_NB` 都立即返回 `EAGAIN`，且旧调用路径
  在操作期间持有 fd-table 锁。test-only `e487eef8` 用父子进程、两个独立 open-file description
  固化阻塞等待、`wait4(WNOHANG)` 与解锁后继续执行的合同，RV/LA guest 均真实 RED；`7597bd9c`
  将等待移出 fd-table 临界区，保留 nonblocking `EAGAIN`，并让 exit/watchdog/未屏蔽 signal
  中断返回 `EINTR`。clean `7597bd9c` quick 47/47、两架构 runtime 各 1/1 PASS。随后确认普通
  rename 的 source 若是 hardlink alias、destination 是不同 inode 的既有文件，旧实现无条件
  `EEXIST`；物理 source 覆盖 hardlink destination 时也会破坏剩余别名的 inode 身份。test-only
  `fdbdb383` 在 RV/LA 均真实 RED；`d8e1055d` 统一清理 unlink 元数据，并按 alias/canonical/
  普通文件类型执行目标删除或 backing promotion，再迁移源 namespace。clean `d8e1055d` quick
  47/47、两架构 runtime 各 1/1 PASS。随后 host rustc trace 明确读取 `/proc/self/statm`，而
  guest 缺失该节点并在 RV/LA test-only `ed5a0773` 均真实 RED；`91a5cb98` 以目标进程 VMA、
  brk、stack 与页表状态生成七字段统计，复用通用 fd `statx(AT_EMPTY_PATH)`，双架构 clean
  runtime 各 1/1、quick 47/47 PASS。两次未通过编译的稀疏页表 walker 尝试均已撤销并记录，
  没有扩大 MM/HAL 或 unsafe 边界。host Cargo/rustc trace 随后确认 56 次 `linkat` 主要把
  `target/debug/deps/*.o` 链接到 incremental `*-working/` 目录，完成后再原子 rename 整个
  working 目录；原实现既不把 metadata hardlink alias 暴露给 `getdents64`，也只迁移被 rename
  的目录自身 metadata。test-only `ee37e940` 在两架构形成 clean `cargo_link_publish` RED，
  `35d1b270` 通用枚举 alias 并迁移目录子树中的全部路径 metadata/canonical 引用；双架构
  clean runtime 各 1/1、quick 47/47 PASS。继续审计又发现底层 VFS 把只含 metadata alias 的
  目标目录视为空，使普通 replacement rename 错误成功；test-only `d904eac6` 在两架构形成
  clean RED，`1b990b8f` 在 namespace 变更前按逻辑 dirent 返回 `ENOTEMPTY`，同时保持
  `RENAME_EXCHANGE` 允许非空目录交换。双架构 clean runtime 各 1/1、quick 47/47 PASS。
  同一 host trace 还显示 rustc 两次创建
  `socketpair(AF_UNIX, SOCK_SEQPACKET|SOCK_CLOEXEC, 0, ...)`，关闭父端不使用的 peer 后以
  `recvfrom(..., flags=0)` 等待子进程 exec/exit；原本地 socket 只接受 stream/dgram，且
  `recvfrom` 在识别本地 socket 前进入 Inet-only 路径。test-only `f19fc9f6` 用短接收丢弃
  当前记录尾部、保留下一完整记录的合同排除“把 seqpacket 当 stream”的假修复，RV/LA clean
  runtime 均明确 `USER_FAIL unix_seqpacket`。`89740c70` 增加有界原子 packet queue、阻塞/
  nonblocking 唤醒、`EMSGSIZE` 与本地 `recvfrom` 路由；首次 clean quick 在 45/47 处因新增
  legacy copyout 库存失败并完整保留。`8c23d29a` 用 typed `UserSlice<u8, Write>` 集中式
  prevalidation/copyout 关闭该边界回归；最终 clean quick 47/47、RV/LA runtime 各 1/1 PASS。
  该修复与 Cargo/rustc 名称和路径无关，正式 toolchain 仍未运行。后续继续验证真实
  rustc/cargo、文件映射、独立顶层 launcher 与更复杂目录 namespace 生命周期。
- [x] 首个可测性能改动已记录前后 elapsed、加速比、正确性和双架构影响；正式 BuildStorm
  elapsed_s 仍须在官方 rootfs 和合格 host 上取得，后续性能批次继续沿用同一证据标准。
- [x] Checkpoint 27 已把 POSIX/OFD record lock、lease 与 `flock` 的共享状态从路径哈希迁移到
  打开 VFS 对象身份。`0c97a5f5` 的 rename 后独立 open 竞争测试在 RV/LA clean runtime 均
  明确 `USER_FAIL flock_rename_identity`；`9ca98b0d` 让 `axfs::File` 保存 resolved node 的存活期
  身份并由三类 key 共用，`4e2e63da` 将新增 mutation 精确注册为 16/16。最终同一 clean commit
  上 RV/LA runtime 各 1/1、quick 47/47 PASS；该合同与 Cargo/BuildStorm 名称或路径无关。
- [x] Checkpoint 28 根据真实 rustc trace 中 `prlimit64(RLIMIT_STACK, 64 MiB)` 定位到通用容量
  缺口：syscall 返回成功，但 loader 只保留 8 MiB 栈 VMA。test-only `f21e419c` 用 fork child
  逐页触碰 12 MiB 栈帧，在 RV/LA clean runtime 均明确 `USER_FAIL rlimit_stack_growth`；
  `1ce29190` 把共享栈边界扩为不预分配物理页的 64 MiB lazy VMA，并补齐 evidence provenance。
  同一 clean commit 上 RV/LA runtime 各 1/1、quick 47/47 PASS。该改动不识别程序名、路径或
  测例；它覆盖实测 64 MiB 工具链需求，但不是任意大小的动态栈增长实现。
- [x] Checkpoint 29 根据真实 Cargo/rustc trace 中 12 次 pipe `ioctl(FIONBIO, [0|1])` 定位到
  通用 fd 状态缺口。test-only `bf053edc` 在 RV/LA clean runtime 均明确
  `USER_FAIL pipe_fionbio`；`cd6c8711` 通过既有 `F_GETFL/F_SETFL` 状态设置或清除
  `O_NONBLOCK`，`d5b606c7` 把新增 copy-in 收敛到 `UserPtr<i32, Read>` typed boundary，
  `427ea835` 补齐 build/runtime evidence provenance。最终同一 clean commit 上 quick 47/47、
  RV/LA runtime 各 1/1 PASS；合同还验证空 pipe 非阻塞 read 返回 `EAGAIN`，不是无条件成功。
- [x] Checkpoint 30 根据真实 Cargo/rustc trace 中 65 次 `prctl(PR_SET_NAME, ...)` 定位到
  `PR_SET_NAME/PR_GET_NAME` 错误共享 `UserProcess` 状态的线程隔离缺口。test-only `2e7becaa`
  让 clone3 worker 设置不同名称，并要求 parent 名称保持不变，在 RV/LA clean runtime 均明确
  `USER_FAIL prctl_thread_name`；`eedc4df3` 将 `comm` 下沉到锁保护的 `UserTaskExt`，实现
  fork/clone 继承、当前线程 prctl 和 `/proc/.../task/<tid>/comm` 读取，`17d30a67` 补齐四组
  build/runtime provenance。最终同一 clean commit 上 RV/LA runtime 各 1/1、quick 47/47 PASS；
  无新增 `unsafe`、依赖、架构或程序名/路径特化。
- [x] Checkpoint 31 根据同一真实 trace 中 227 次 `sigaltstack` 定位到 task lifecycle 缺口：
  fork-like child 总被初始化为禁用，成功 exec 又不清除旧状态。test-only `5d1e2581`/
  `f28dc367` 通过真实 `stack_t` syscall、普通 clone3 child、vfork pre-exec 汇编查询与独立 exec
  ELF 建立合同；最终 clean RED 在 RV/LA 均明确 `USER_FAIL sigaltstack_fork_exec`。
  `915cafd1` 让 fork 与 `CLONE_VM|CLONE_VFORK` 继承调用线程备用栈，保留普通 CLONE_VM thread
  的禁用规则，并在成功 exec 后清除备用栈和旧 image 的瞬态 signal/restart frame。同一 clean
  commit 上 RV/LA runtime 各 1/1、quick 47/47 PASS；无新增依赖、`unsafe`、ABI、架构或
  程序名/路径特化。
- [x] Checkpoint 32 继续审计相同 `sigaltstack` 真实调用，定位到 `SA_ONSTACK` 只被保存、
  实际 signal frame 始终仍放在普通用户栈的通用缺口。test-only `5ab91a01` 用原始
  `rt_sigaction`/`kill`/`sigaltstack` 和真实 `extern "C"` handler 同时验证局部变量地址与
  handler 内查询到的 `SS_ONSTACK`；RV/LA clean RED 均明确
  `USER_FAIL sigaltstack_onstack`。`066c4ade` 仅同步 synthetic evidence fixture；
  `36584570` 以共享 checked frame-layout helper 在尚未位于备用栈时选择备用栈顶，写入真实
  `ucontext.uc_stack`，并按 signal-frame 地址计算 `SS_ONSTACK`/`EPERM`。同一 clean commit
  上 RV/LA runtime 各 1/1、quick 47/47 PASS；无新增依赖、`unsafe`、syscall、架构或
  程序名/路径特化。
- [x] Checkpoint 33 根据真实 Cargo/rustc host trace 定位到通用文件映射性能缺口：动态
  loader 对 123,663,556-byte `librustc_driver` 段使用普通 `MAP_PRIVATE|MAP_FIXED`，旧
  `sys_mmap` 却同步读取并分配整个文件范围。test-only `0f37d27e` 用 16 MiB 稀疏普通文件与
  `mincore` 固化“映射后 0 页驻留、仅访问的首尾 2 页驻留、private 写不回 backing”，RV/LA
  clean RED 均明确 `USER_FAIL mmap_private_lazy`；`35a65b75` 又要求 nonresident mapping 上
  `msync(MS_SYNC)` 成功且不预取，并要求 `MADV_DONTNEED` 丢弃 private 修改后从 backing
  重载。`c2258c56` 让未锁定、未请求有效 `MAP_POPULATE` 的 private 文件映射按页 fault-in，
  保持 shared/locked/populated eager，I/O 在地址空间锁外完成并在安装页前重验 VMA/backing；
  同时按 VMA 验证 `msync`、为 private file 实现 discard/reload，并在 `mremap` 移动时只复制
  已驻留 private 页。同一 clean commit 上 RV/LA runtime 各 1/1、quick 47/47 PASS；无新增
  `unsafe`、依赖、syscall、架构或程序名/路径特化。正式 BuildStorm elapsed/分数仍未运行。
- [x] Checkpoint 34 继续按真实 Cargo/rustc trace 的高频 `FUTEX_PRIVATE` 形态审计，发现
  futex table 无条件按物理地址键控：fork 后的进程若在 COW private page 上创建线程，waiter
  会排入旧 frame，sibling 首次 store 触发 COW 后 wake 却查找新 frame。test-only `53656508`
  用 pre-fork private anonymous page、exact clone3 worker 与有界 private wait/wake 在 RV/LA
  均得到明确 `USER_FAIL private_futex_cow`；`0adfc175` 将 private key 改为共享地址空间身份加
  用户虚址，shared futex 保持 backing frame identity，并让 signal/exit-group 按 task 在两类
  队列中精确中断。clear-child-tid 保持 shared wake，未破坏既有 glibc join。最终 clean
  RV/LA runtime 各 1/1、quick 47/47 PASS；无新增 production `unsafe`、依赖、syscall、ABI、
  架构或程序名/路径特化。正式 BuildStorm 仍未执行或计分。
- [x] Checkpoint 35 继续从同一真实 host trace 定位信号中断后的 futex 重启缺口：Linux host
  在 `FUTEX_WAIT_BITSET_PRIVATE` 被 signal 中断后进入 `restart_syscall`，OrayS 旧实现却只把
  `wait4`/`waitid` 纳入 `SA_RESTART`。test-only `607b0379` 以共享匿名 futex、绝对
  `CLOCK_MONOTONIC` deadline、真实 `SIGUSR1|SA_RESTART` handler 与真实 shared wake 在
  RV/LA 均得到明确 `USER_FAIL futex_sa_restart`；`991f5856` 仅把超时为 absolute（或空）的
  `FUTEX_WAIT_BITSET` 纳入现有 frame replay，未对 relative-timeout `FUTEX_WAIT` 做会延长
  deadline 的不诚实重放。最终 clean RV/LA runtime 各 1/1、quick 47/47 PASS；无新增
  production `unsafe`、依赖、syscall、ABI、架构或程序名/路径特化。relative wait 仍需带
  remaining-time 状态的 restart block，正式 BuildStorm 仍未执行或计分。
- [x] Checkpoint 36 闭合 relative `FUTEX_WAIT` 的 remaining-time restart：test-only
  `4eef0eb8` 让 child 以 1.5 秒相对 timeout 等待、parent 约 0.75 秒发送真实
  `SIGUSR1|SA_RESTART` 且不 wake，要求 handler 运行、最终 `ETIMEDOUT` 且总耗时仍落在原
  deadline 窗口；因此既能拒绝旧实现的即时 `EINTR`，也能拒绝把完整 1.5 秒重新计时的伪
  restart。RV/LA dirty supervised 均明确 `USER_FAIL futex_relative_restart`。`fb889572`
  增加按 task 保存的精确 futex 参数键、内核单调 deadline 与 armed 状态，只在匹配的
  relative wait 返回 `EINTR` 后允许 replay，并在 handler syscall、`rt_sigreturn`、普通完成、
  非 restart signal、exec/exit 间保持或清理正确生命周期。最终 clean RV/LA runtime 各
  1/1、quick 47/47 PASS；无新增 production `unsafe`、依赖、syscall、ABI、架构或程序名/
  路径特化。正式 BuildStorm 仍未执行或计分。
- [ ] 每个逻辑批次在 clean commit 上跑 quick/baseline；终态跑 full-all。

## 5. 关键决策、风险与回滚

| 决策 | 理由 | 风险 | 回滚 |
|---|---|---|---|
| final profile 与旧 24-group official 分离 | 两套镜像、协议、资源和评分完全不同 | profile 数量增加 | 逻辑提交普通 revert，不影响旧入口 |
| 解析结构完整性后再算分 | judge 对重复/缺失文本较宽松，canonical evidence 必须 fail closed | 与 reference judge 输出需同时维护 | 单元 fixture 固定两者一致性，不改外部 judge |
| 镜像缺失时继续 runner/回归工作 | 外部 artifact 不应阻断全部可完成工作 | 无法形成官方 delta | 明确记录 BLOCKED，镜像到位后从固定命令恢复 |
| BuildStorm 不在 2c/3.6 GiB 主机伪跑 8c/8G | 资源不满足官方计分合同，可能宿主 OOM | 暂无真实 BuildStorm 分数 | 在合格主机用同一提交和只读镜像重跑 |
| semantic smoke 固定为 8 vCPU | BuildStorm 以 `nproc` 要求 guest 真实看到 8 核，单核 smoke 无法发现 affinity 伪语义 | 本地 QEMU 调度开销略增 | `PR3_SMOKE_SMP` 可显式覆盖；默认值的逻辑提交可普通 revert |
| ext4 lower 保持不可变，所有根盘写入进入 ramfs upper | 官方镜像是只读输入，但 BuildStorm 必须在 Debian rootfs 的一般目录真实写入 | upper 消耗 guest RAM，重启后不持久化 | 普通 revert `5972629e` 恢复显式只读 ext4；正式运行继续使用 qcow2 防护 backing |
| overlay namespace 与打开对象分离，并以弱引用缓存活跃路径 | Linux rename/unlink 只改变目录项，不能让已打开文件描述符失效或错误指向新对象 | 活跃 lower 文件在 unlink 时需先 copy-up；缓存与 rename 需要串行一致性 | 普通 revert `0f985e28` 回到路径解析实现；对应 RED 合同会重新失败 |
| VFS 为所有节点提供 no-follow metadata/readlink 虚接口，ext4/overlay 实现真实委托 | 动态 toolchain rootfs 依赖一般符号链接；只在用户态猜测目标会破坏 inode 类型与 errno | 当前只补读取/检查既有 lower link，未把 symlink 创建统一迁入 overlay upper | 普通 revert `639d8034`；test-only `18a305d6` 会恢复为编译期 RED |
| final suite 目录由 provenance 绑定的 guest script 派生，普通 POSIX shell 与 BusyBox provider 分离 | BuildStorm 位于 `/scripts` 的 Debian rootfs，旧 autorunner 只扫描 libc suite 且按 BusyBox 调用所有 shell | 新路径尚无正式 final image runtime 证据；错误配置必须 fail closed | 普通 revert `bae025db`；四组 test-only launch contract 会恢复 RED |
| rootfs 动态 ELF 保留 `/` runtime root，标准 `/lib*` 先于旧 suite compatibility root 搜索 | Debian loader 可用裸 SONAME 打开 `libc.so.6`；按解释器 basename 强制 `/glibc` 会让 LA64 标准布局退出 127 | 旧 `/musl`、`/glibc` fallback 顺序必须保持；尚未覆盖正式 rustc/cargo | 普通 revert `adacda8c`；test-only `007ae7aa` 与 mutation `66a453fe` 会恢复 RED |
| fork 共享路径元数据 namespace，而非深拷贝进程私有快照 | 文件创建可落入共享 VFS，但 symlink/hardlink/权限/xattr 等模拟元数据若不共享，会让子进程成功操作对父进程不可见 | 共享锁会增加父子并发竞争；独立顶层 launcher 之间的元数据生命周期仍需继续审计 | 普通 revert `1b5aa307`；test-only `ab2d9880` 会恢复 RED |
| 删除 hardlink canonical 名时把真实 backing 原子提升到剩余别名，rename 时同步 canonical 映射 | Linux hardlink 的 inode 生命周期不能依赖最初创建链接时的路径名；复制数据或直接删除 backing 都不符合语义 | 当前 hardlink 仍是共享路径元数据模型而非底层 VFS inode link；跨独立 launcher 与更复杂覆盖目标仍需审计 | 普通 revert `ef3d6ef9`；test-only `74b1a4e1` 会恢复 RED |
| 阻塞 `flock` 在 fd-table 临界区外重试，并保留 `LOCK_NB`/`EINTR` 分支 | Cargo 等并发工具依赖竞争锁等待；持有 fd-table 锁等待会阻止同进程其他 fd 操作，立即 `EAGAIN` 则违反未指定 `LOCK_NB` 的语义 | 当前采用 cooperative poll/yield 而非专用 wait queue，竞争密集时有额外调度开销 | 普通 revert `7597bd9c`；test-only `e487eef8` 会恢复双架构 RED |
| 普通 rename 覆盖时按目标 namespace 身份删除或提升 hardlink backing | 不带 `RENAME_NOREPLACE` 的 rename 必须替换不同 inode 目标，同时保留目标 inode 的其他 hardlink；无条件 `EEXIST` 或让 alias 改指新 inode 都错误 | recorded hardlink 模型仍非底层 VFS inode，复杂并发 rename 的原子可见性需继续审计 | 普通 revert `d8e1055d`；test-only `fdbdb383` 会恢复双架构 RED |
| `/proc/<pid>/statm` 从 VMA/brk/stack 与当前页表生成快照 | rustc 真实读取七字段并对 fd 做 `statx(AT_EMPTY_PATH)`；返回固定值或 host 数据都不符合进程可见语义 | 当前 resident 查询按已合并可见页逐页进行；正式大进程性能仍需真实 rootfs 验证 | 普通 revert `91a5cb98`；test-only `ed5a0773` 会恢复双架构 open RED |
| metadata hardlink alias 参与目录枚举，目录 rename 迁移整棵路径 metadata 子树 | rustc 先把对象 hardlink 到 incremental working 目录，再以目录 rename 发布；若 alias 不可枚举或 metadata 留在旧前缀，发布目录会丢失构建产物 | hardlink 仍是用户态 metadata 模型；跨独立顶层 launcher 与复杂并发目录操作需继续审计 | 普通 revert `35d1b270`；test-only `ee37e940` 会恢复双架构 `cargo_link_publish` RED |
| 普通 replacement rename 在底层 VFS emptiness 之外检查 metadata-backed 逻辑 dirent | Linux 不允许普通 rename 覆盖非空目标目录；只检查物理 VFS 会错误删除或混合 symlink/hardlink alias | 当前查询在 metadata 锁的独立快照间完成；复杂并发目录 mutation 的整体原子性仍需继续审计 | 普通 revert `1b990b8f`；test-only `d904eac6` 会恢复双架构 `rename_virtual_nonempty` RED |
| AF_UNIX packet socket 使用独立有界记录队列，并让本地 `recvfrom` 走 typed copyout | `SOCK_SEQPACKET` 必须原子写入、按记录读取并在短接收后丢弃余部；rustc 真实用 CLOEXEC peer EOF 同步子进程 | `MSG_PEEK/MSG_TRUNC` 与 pathname peer-address copyout 仍 fail-closed；正式 Cargo 需 final rootfs 验证 | 普通 revert `8c23d29a`、`89740c70`；test-only `f19fc9f6` 会恢复双架构 RED |
| 文件锁、record lock 与 lease 绑定 resolved VFS node，而非打开时 pathname | rename/unlink 只改变目录项；旧 fd 与 rename 后的新 open 必须继续竞争同一对象，同路径 replacement 又必须是不同对象 | opaque identity 依赖 VFS 对同一存活对象复用 node；已由 overlay 双架构 runtime 与静态 mutation 覆盖，其他 filesystem backend 仍需定向验证 | 普通 revert `9ca98b0d`；test-only `0c97a5f5` 会恢复双架构 RED |
| 用户栈以共享边界保留 64 MiB lazy VMA | rustc 真实先把 `RLIMIT_STACK` 提到 64 MiB；接受 limit 却只映射 8 MiB 会在一般深栈/大栈帧工作负载中 SIGSEGV | 当前是固定 64 MiB 上限而非按 rlimit 任意动态增长；默认报告值也随共享常量变为 64 MiB | 普通 revert `1ce29190`；test-only `f21e419c` 会恢复双架构 RED |
| `sigaltstack` 按调用 task 在 fork/vfork 继承、成功 exec 清除 | Linux 明确定义 fork copy、`CLONE_VM`/`CLONE_VFORK` 例外及 exec removal；Cargo/rustc host trace 高频安装/禁用备用栈 | 从信号 handler 内 fork 时的动态 `SS_ONSTACK`/signal-frame 继承仍需单独验证；正式 toolchain 仍依赖 final rootfs | 普通 revert `915cafd1`；test-only `5d1e2581`/`f28dc367` 会恢复双架构 RED |
| `SA_ONSTACK` 按真实备用栈范围选择 signal frame，并从 frame 地址派生 `SS_ONSTACK` | Linux handler 必须真正运行于已启用备用栈，handler 内查询与修改权限取决于是否实际处于该栈；仅保存 flag 会造成错误安全边界 | `SS_AUTODISARM` 的 handler 期间临时禁用与 context restore、嵌套 signal 仍需独立审计；正式 toolchain 仍依赖 final rootfs | 普通 revert `36584570`；test-only `5ab91a01` 会恢复双架构 RED |
| 普通 private 文件映射按访问页读取，shared/locked/显式 populate 保持 eager | rustc 动态 loader 会映射百 MiB 级只读 SO；在 `mmap` 时复制完整文件会把启动 I/O 与 RSS 乘以并发 compiler 数，且违反一般 demand-paging 行为 | backing 有效长度当前在建图时快照；映射后的 truncate/grow、扩展 `mremap` 与更多 invalidate 组合仍需后续定向审计 | 普通 revert `c2258c56`；test-only `0f37d27e`/`35a65b75` 会恢复双架构 RED |
| private futex 以共享地址空间身份和虚址键控，shared futex 继续以物理地址键控 | `FUTEX_PRIVATE_FLAG` 的 Linux 合同是同一 mm 内同步；物理键会在 private COW remap 后拆散同一线程组的 wait/wake | futex 比较到排队之间的原子性与 requeue 竞争仍需独立并发审计；正式 toolchain 压力尚未运行 | 普通 revert `0adfc175`；test-only `53656508` 会恢复双架构 RED |
| 仅把 `FUTEX_WAIT_BITSET` 纳入现有 `SA_RESTART` frame replay | 该操作的 timeout 是 absolute，重放原始参数不会延长 deadline；无 timeout 时同样安全 | relative-timeout `FUTEX_WAIT` 不能直接重放；该风险已由后续 Checkpoint 36 的 deadline 状态闭合 | 普通 revert `991f5856`；test-only `607b0379` 会恢复双架构 RED |
| relative `FUTEX_WAIT` 按 task 保存精确参数键和内核单调 deadline，只在匹配 `EINTR` 后 armed | signal handler 用时必须从原相对 timeout 中扣除，同时 handler 内 futex、嵌套 frame、普通完成、exec/exit 不能消费或泄漏外层 restart 状态 | futex requeue、比较到排队的原子性及更复杂 signal/timeout 竞争仍需独立压力审计 | 普通 revert `fb889572`；test-only `4eef0eb8` 会恢复双架构 RED |

## 6. 当前下一步

final parser、adapter、四格 canonical profile 与缺镜像 fail-closed 矩阵均已落地。双架构
semantic smoke 已闭环动态 `/proc/uptime`、`/dev/null` access mode、真实 task affinity、
版本化 `clone3` process ABI、Cargo/glibc exact worker-thread ABI、glibc clear-child-tid
futex join、`posix_spawn` vfork/exec ABI、备用信号栈的 fork/vfork 继承、exec 清除与
`SA_ONSTACK` 真实 handler 栈投递、
`MADV_DONTNEED` 的真实 page discard/refault/
fork-COW 隔离，以及只读 ext4 lower 上的通用 volatile writable overlay；overlay 打开的
源文件、lower 文件、被覆盖目标文件和嵌套目录也已形成 rename/unlink 后对象身份不变的 host
合同。CAgent 审计补齐
BusyBox `df`/`nproc` 的真实 PATH wrapper，并验证并发 TCP/fork 与
pipe/fork/execve/wait4 链路。clean `e311d98d` 上 quick 47/47、RV/LA runtime 各 1/1 PASS；
两次因测试 child-stack slot 覆盖产生的 clean FAIL 已保留并明确归因，没有改写为内核回归。
本轮 vfork 与 futex 精确形态在新增测试前已有正确生产语义，因此只增加风险覆盖，不宣称
分数 delta；MADV_DONTNEED 则在同一通用 workload 上形成了可复算的双架构性能 delta，但
仍不等于官方 BuildStorm 得分。`5972629e` 的 overlay 又在旧的只读 ext4 镜像上完成双架构
真实创建/读取，RV 还清晰验证 lower 文件 copy-up、whiteout 与同路径重建；同一 clean commit
quick 47/47、RV/LA runtime 各 1/1 PASS。句柄生命周期已在 test-only `8fdcf4bc`、
`1f7c14ef` 两次真实 RED 后由 `0f985e28` 闭环；同一 clean commit 上 quick 47/47、RV/LA
runtime 各 1/1 PASS。lower ext4 符号链接合同又在 test-only `18a305d6` 编译期 RED 后由
`639d8034` 闭环，并经 fresh ext4 RV/LA guest 验证 `readlink`、no-follow/follow metadata 与
真实 exec resolution；首次 clean quick 诚实保留为 45/47 FAIL，原因是新增 copyout 使边界库存
增长。后续 `a4d25d56` 用 typed `UserSlice` 合并该写出路径，将旧式调用库存从 47 收紧到 45；
同一 clean commit 上 quick 47/47、RV/LA runtime 各 1/1 PASS。下一步继续审计动态 toolchain
和其余通用文件系统能力。final 启动链路已在 `154c4bad`、`7cf8ab03`、`05d4e21c`、
`9d93ab27` 的测试先行合同后由 `bae025db` 闭环，`b5616bbd` 修复其 canonical identity 注册；
同一 clean commit quick 47/47、RV/LA runtime 各 1/1 PASS。随后 Debian 标准根动态 ELF
探针在 LA64 真实复现 `libc.so.6` 加载失败，`007ae7aa` -> `adacda8c` 已形成通用
RED/GREEN，修复后 RV/LA 同一载荷均退出 0；这仍不是正式 toolchain PASS。随后跨进程
hardlink 探针在 RV/LA 均复现父进程 `ENOENT`，`ab2d9880` -> `1b5aa307` 通过 fork 共享 11 类
路径元数据闭环，同一载荷双架构均 PASS；随后 canonical unlink/rename 生命周期由
`74b1a4e1` -> `ef3d6ef9` 闭环，双架构真实 ext4 guest 同时验证剩余别名与新名称，clean quick
47/47、runtime 各 1/1 PASS。随后 `e487eef8` 在 RV/LA 都证明竞争的阻塞 `flock` 被错误地
立即拒绝，`7597bd9c` 在不持有 fd-table 锁的情况下等待并保留 `LOCK_NB`/`EINTR` 语义；同一
clean commit 上 quick 47/47、runtime 各 1/1 PASS。下一步优先审计文件映射、`st_nlink`、
复杂 rename 覆盖和独立顶层 launcher 的 namespace 生命周期，并在不冒充 final 的最小通用
guest 中形成 RED/GREEN。hardlink alias 覆盖既有目标又由 `fdbdb383` -> `d8e1055d` 完成
双架构 RED/GREEN：目标内容切换到源 inode、原源仍可读、旧 alias 消失，clean quick 47/47。
host rustc 的最小 syscall trace 随后确认 `/proc/self/statm` 是实际读取项，test-only
`ed5a0773` 在 RV/LA 均以 `ENOENT` 形成 clean RED；`91a5cb98` 用目标进程真实 VMA/brk/stack
与页表状态实现七字段快照，同一 clean commit 上两架构 runtime 各 1/1、quick 47/47 PASS。
host trace 中 56 次 Cargo/rustc `linkat` 又定位到 incremental working-directory publish：
test-only `ee37e940` 在 RV/LA 均因 alias 未被 readdir 发现而形成 clean RED；`35d1b270`
让 hardlink alias 作为普通文件参与 `getdents64`，并在目录普通 rename/exchange 时迁移所有
后代 metadata、hardlink canonical 与 link count。相同 clean commit 上两架构 runtime 各
1/1、quick 47/47 PASS。随后只含 metadata alias 的目标目录仍会被底层 VFS 误判为空：
test-only `d904eac6` 在 RV/LA 均形成 clean replacement-rename RED；`1b990b8f` 对普通 rename
按逻辑 dirent 返回 `ENOTEMPTY`，保留 exchange 语义，同一 clean commit 上双架构 runtime
各 1/1、quick 47/47 PASS。host rustc 的真实 `SOCK_SEQPACKET|SOCK_CLOEXEC` 同步形态又由
`f19fc9f6` -> `89740c70` -> `8c23d29a` 完成双架构 RED/GREEN；短接收记录边界、非阻塞空队列、
peer close EOF、`SO_TYPE` 与 fd/status flags 均通过，最终 clean quick 47/47。下一步继续沿
host toolchain trace 审计 mmap/readlink/stat/rename 与独立 launcher namespace，优先选择可在
通用双架构 guest 中形成不冒充正式分数的真实 RED/GREEN 项。随后路径哈希锁键在 rename
场景被证实违反 Linux 对象语义：`0c97a5f5` 双架构 clean RED 后，`9ca98b0d`/`4e2e63da`
以存活 VFS node identity 闭环三类对象级状态，并完成 RV/LA runtime 各 1/1、quick 47/47
clean GREEN。随后真实 host rustc trace 又确认主线程把 `RLIMIT_STACK` 从 8 MiB 提到
64 MiB，而 guest 旧 loader 永久只保留 8 MiB VMA：`f21e419c` 在 RV/LA 均以 child 非零退出
形成 clean RED，`1ce29190` 扩大 lazy 栈保留后同一逐页触碰合同双架构均 GREEN，clean quick
47/47。随后 `5ab91a01` 又证明已登记的 `SA_ONSTACK` 从未改变 signal frame 栈地址；
`36584570` 用同一套双架构 frame-layout 合同完成真实 handler 栈、`uc_stack` 与
`SS_ONSTACK`/`EPERM` 闭环，并在 clean RV/LA runtime 与 quick 上全部通过。
真实 host trace 随后显示 loader 用普通 private fixed mapping 映射 123,663,556-byte
`librustc_driver` 段，而 guest 旧实现同步读入完整范围；`0f37d27e`/`35a65b75` -> `c2258c56`
已用双架构 16 MiB 稀疏文件的 0 -> 2 resident page、`msync` no-prefetch 与
`MADV_DONTNEED` backing reload 合同完成 clean RED/GREEN，最终 quick 47/47。下一步继续审计
文件映射 truncate/grow/扩展 `mremap`、`statx`、`SS_AUTODISARM`/嵌套信号、锁与
replacement/unlink 的组合生命周期及独立顶层 launcher，并把固定 64 MiB 栈容量视为已知边界
而非完整动态增长；`rseq` 在缺少迁移、抢占与
signal abort 完整语义时保持诚实 `ENOSYS`，不以伪注册换取表面成功。
同一 host trace 的 private futex/COW 身份缺口随后由 `53656508` -> `0adfc175` 完成双架构
RED/GREEN；clean RV/LA runtime 各 1/1、quick 47/47 PASS。后续 futex 工作优先审计比较与
排队的原子性、requeue 和 signal/timeout 竞争，不把当前 key 修复外推为完整压力测试通过。
紧接着 `607b0379` -> `991f5856` 又闭合 absolute `FUTEX_WAIT_BITSET` 的 `SA_RESTART`：两架构
均先形成真实 signal 中断 RED，再在相同通用合同上 clean GREEN，quick 47/47。随后
`4eef0eb8` -> `fb889572` 以真实 handler 用时、原 deadline 内 `ETIMEDOUT` 和双架构计时窗口
闭合 relative `FUTEX_WAIT` 的 remaining-time replay；clean RV/LA runtime 各 1/1、quick
47/47 PASS。下一步继续审计 futex compare/enqueue、requeue 返回值与 requeue/signal/timeout
竞争，不把单次行为合同外推为完整并发压力通过。
公开官方
share 可列出两个 gzip 文件，
但匿名第一方接口未提供可审计下载 URL；正式镜像或合格 8c/8G host 到位前，正式得分保持
BLOCKED/未执行。
