# OrayS 执行计划

本目录保存跨模块、跨提交或高风险工作的可审查执行计划。计划应在实施前建立，并在实施过程中持续更新真实状态、风险、验证结果和重要决策。

- `active/`：尚未满足全部验收条件的计划；失败、阻塞和待复核项必须保留。
- `completed/`：验收条件全部有证据后，从 `active/` 使用 `git mv` 移入；完成记录不得删除失败历史。

计划至少包含目标、非目标、阶段、风险、验证方法、关键决策、回滚方式和当前状态。大体积原始输出放在已忽略的 `test/output/` 或 CI artifact 中。
