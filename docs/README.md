# Documentation entry points

This directory is kept intentionally small for review.

Active entry points:

- `agent-workflow/` — agent workflow, validation, branch, and collaboration rules.
- `developer-guide/` — current repository map, build/evaluation guidance, and validation matrix.
- `ltp-full-sweep-blacklist-2026-05-30-arch/` — active supplemental blacklist input referenced by `Makefile`.
- `ltp-real-semantics-repair-2026-06-07/` — active self-check/LTP repair evidence referenced by regression guard scripts.
- `archive/` — historical sprint notes, dated reports, old evaluator outputs, and pre-review archives.

Historical top-level dated reports were moved to `archive/pre-review-docs-2026-06-27/` before review to keep the root documentation surface focused.
