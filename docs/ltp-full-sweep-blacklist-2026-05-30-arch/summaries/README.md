# Summaries

This directory stores compact parser/marker artifacts for the 2026-05-30 RV/LA arch-specific blacklist closure run.  Raw logs stay under `target/ltp-full-sweep-blacklist-2026-05-30-arch/raw/` and are not committed by default.

Final closed runs:

- RV: `rv-arch002-summary.md` / `rv-arch002-summary.json`, raw sha256 in `rv-arch002.log.sha256`.
- LA: `la-arch012-summary.md` / `la-arch012-summary.json`, raw sha256 in `la-arch012.log.sha256`.

Use `../final-report.md` for the durable result and caveats.  Blacklisted/skipped cases are exclusions only, not PASS or stable-promotion evidence.
