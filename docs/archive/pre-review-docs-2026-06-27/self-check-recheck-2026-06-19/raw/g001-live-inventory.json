{
  "stable_ltp_count": 1000,
  "stable_ltp_unique": 1000,
  "stable_ltp_duplicates": [],
  "stable_ltp_first20": [
    "access01",
    "brk01",
    "chdir01",
    "clone01",
    "close01",
    "dup01",
    "fcntl01",
    "fcntl02",
    "fork01",
    "getpid01",
    "mmap01",
    "open01",
    "pipe01",
    "read01",
    "stat01",
    "wait401",
    "write01",
    "access03",
    "close02",
    "dup02"
  ],
  "stable_ltp_last20": [
    "mesgq_nstest",
    "mmap18",
    "mmapstress01",
    "mmapstress02",
    "mmapstress03",
    "mmapstress05",
    "mount07",
    "pipeio",
    "realpath01",
    "sbrk01",
    "sem_nstest",
    "semtest_2ns",
    "sendmsg02",
    "splice06",
    "tee01",
    "tee02",
    "vmsplice01",
    "vmsplice02",
    "vmsplice03",
    "vmsplice04"
  ],
  "suite_priority_strings": [
    "cyclictest",
    "iozone",
    "lmbench",
    "netperf",
    "unixbench",
    "libctest",
    "basic",
    "busybox",
    "lua",
    "iperf",
    "netperf",
    "unixbench",
    "libcbench",
    "lmbench",
    "cyclictest",
    "iozone",
    "ltp"
  ],
  "non_ltp_candidate_strings_count": 55,
  "non_ltp_candidate_strings_sample": [
    " && fs::metadata(&join_path(src_root, dep)).is_ok()\n                    }),\n            );\n        }\n    }\n\n    copy_runtime_libs(src_root, &stage_root, &busybox_path)?;\n\n    Ok(Some(stage_root))\n}\n\n#[cfg(all(feature = ",
    " && matches!(fs::metadata(musl_busybox), Ok(meta) if meta.is_file()) {\n        musl_busybox.into()\n    } else {\n        busybox_path.into()\n    }\n}\n\n#[cfg(all(feature = ",
    " => 10,\n        // LTP is intentionally last: the stable list is still large enough that\n        // running it before stress/throughput groups can starve later official\n        // non-LTP groups or the second libc's LTP phase on remote evaluation.\n        ",
    " {\n            continue;\n        }\n        copy_stage_entry(src_root, &stage_root, &rel, &busybox_path)?;\n        if metadata.is_file() && rel.ends_with(",
    " {\n            if let Err(err) = run_busybox_suite(&cwd, &suite_dir) {\n                println!(",
    " {\n            if let Err(err) = run_libctest_suite(&suite_dir, &cwd) {\n                println!(",
    " {\n        return Ok(None);\n    }\n\n    let src_root = suite_dir;\n    let busybox_path = join_path(suite_dir, ",
    "#!{busybox_path} sh",
    "$({busybox_path} {applet}",
    ")\n            };\n            copy_stage_entry(src_root, dst_root, &child_rel, busybox_path)?;\n        }\n    } else if rel.ends_with(",
    ")\n        };\n        let path_dir = unstaged_script_dir.as_deref().unwrap_or(&cwd);\n        if let Err(err) = ensure_busybox_path_wrappers(path_dir, &shell_path) {\n            println!(",
    ")\n        };\n        match run_user_program_argv_in(cwd, &[&busybox_path, ",
    ") {\n        copy_script_file(&src, &dst, busybox_path, !rel.contains('/'))?;\n    } else {\n        copy_file(&src, &dst)?;\n    }\n    Ok(())\n}\n\n#[cfg(all(feature = ",
    "))?;\n    let chmod_args = busybox_path_wrapper_chmod_args(cwd);\n    let commands = fs::read_to_string(&join_path(cwd, ",
    "))]\n// Stable LTP contains real file-stress cases (for example ftest03/ftest07)\n// that regularly take tens of seconds on the single-vCPU evaluator and can\n// exceed 90s on slower LA64/QEMU hosts or when both architectures are under\n// verification load. Keep this above their honest runtime while still bounding\n// genuine hangs.\nconst LTP_CASE_TIMEOUT_SECS: u64 = 180;\n\n#[cfg(all(feature = ",
    "))]\nfn busybox_path_wrapper_chmod_args(dir: &str) -> String {\n    PATH_BUSYBOX_APPLETS\n        .iter()\n        .map(|applet| join_path(dir, applet))\n        .collect::<Vec<_>>()\n        .join(",
    "))]\nfn copy_runtime_libs(src_root: &str, stage_root: &str, busybox_path: &str) -> io::Result<()> {\n    let lib_dir = join_path(src_root, ",
    "))]\nfn copy_stage_entry(\n    src_root: &str,\n    dst_root: &str,\n    rel: &str,\n    busybox_path: &str,\n) -> io::Result<()> {\n    let src = join_path(src_root, rel);\n    let dst = join_path(dst_root, rel);\n    let metadata = fs::metadata(&src)?;\n    if metadata.is_dir() {\n        ensure_dir_all(&dst)?;\n        for entry in fs::read_dir(&src)? {\n            let entry = entry?;\n            let file_name = entry.file_name();\n            let name = String::from(path_to_str(&file_name));\n            let child_rel = if rel.is_empty() {\n                name\n            } else {\n                format!(",
    "))]\nfn ensure_busybox_path_wrappers(dir: &str, busybox_path: &str) -> io::Result<()> {\n    if !matches!(fs::metadata(busybox_path), Ok(meta) if meta.is_file()) {\n        return Ok(());\n    }\n    for applet in PATH_BUSYBOX_APPLETS {\n        create_busybox_applet_wrapper(dir, busybox_path, applet)?;\n    }\n    Ok(())\n}\n\n#[cfg(all(feature = ",
    "))]\nfn ltp_case_env(\n    suite_dir: &str,\n    target_dir: &str,\n    needs_case_resource_helper: bool,\n) -> Vec<String> {\n    let mut env = vec![\n        // Keep the current run directory and testsuite bin directory first for\n        // resource helpers, then rely on the runtime's /musl and /glibc\n        // busybox-applet aliases for shell tools such as cp/chmod/awk.  Avoid\n        // putting /tmp helper scripts ahead of those aliases: ramfs-created\n        // files default to 0666, and chmod metadata is per-process in this\n        // userspace model, so those wrappers can shadow working applet aliases\n        // with EACCES inside LTP's system(",
    "))]\nfn ltp_helper_busybox_path(suite_dir: &str, busybox_path: &str) -> String {\n    let musl_busybox = ",
    "))]\nfn prepare_ltp_helper_bin(suite_dir: &str, busybox_path: &str) -> io::Result<String> {\n    let helper_dir = join_path(\n        TESTSUITE_STAGE_ROOT,\n        &format!(",
    "))]\nfn prepare_unstaged_script_dir(\n    suite_dir: &str,\n    group: &str,\n    script_name: &str,\n    busybox_path: &str,\n) -> io::Result<String> {\n    let stage_root = join_path(\n        TESTSUITE_STAGE_ROOT,\n        &format!(",
    "))]\nfn run_busybox_suite(cwd: &str, suite_dir: &str) -> Result<(), String> {\n    let label = suite_label(suite_dir, ",
    "))]\nfn run_libctest_suite(suite_dir: &str, cwd: &str) -> Result<(), String> {\n    let label = suite_label(suite_dir, ",
    ");\n                continue;\n            }\n        };\n        let use_staged_dir = staged_dir.is_some();\n        let suite_busybox = join_path(&suite_dir, ",
    ");\n                failed += 1;\n                continue;\n            }\n        };\n        for line in script.lines() {\n            let Some((entry, case)) = parse_libctest_command(line.trim()) else {\n                continue;\n            };\n            seen_commands += 1;\n            let entry_arg = format!(",
    ");\n            }\n            if use_staged_dir {\n                let _ = remove_dir_all(&cwd);\n            }\n            if let Some(dir) = unstaged_script_dir {\n                let _ = remove_dir_all(&dir);\n            }\n            continue;\n        }\n        let chmod_args = busybox_path_wrapper_chmod_args(path_dir);\n        let command = format!(\n            ",
    ");\n        let suite_shell = if matches!(fs::metadata(&suite_busybox), Ok(meta) if meta.is_file()) {\n            suite_busybox.as_str()\n        } else {\n            shell\n        };\n        let (cwd, shell_path) = if let Some(dir) = staged_dir {\n            (dir, suite_busybox)\n        } else {\n            (suite_dir.clone(), suite_shell.to_string())\n        };\n        if let Err(err) = std::env::set_current_dir(&cwd) {\n            println!(",
    ");\n    ensure_busybox_path_wrappers(cwd, &busybox_path)\n        .map_err(|err| format!(",
    ");\n    let _helper_dir = prepare_ltp_helper_bin(suite_dir, &busybox_path)\n        .map_err(|err| format!(",
    ");\n    let busybox_path = join_path(suite_dir, ",
    ");\n    let timeout_secs = LIBCTEST_CASE_TIMEOUT_SECS;\n    let mut passed = 0usize;\n    let mut failed = 0usize;\n    let mut timed_out = 0usize;\n    let mut seen_commands = 0usize;\n\n    println!(",
    ", &busybox_path);\n        let command = if line.starts_with(&busybox_path) {\n            format!(",
    ", &command],\n                timeout_secs,\n            )\n        } else {\n            let mut argv = Vec::with_capacity(env.len() + 3);\n            argv.push(busybox_path.as_str());\n            argv.push(",
    ", busybox_path)?;\n    }\n    Ok(())\n}\n\n#[cfg(all(feature = ",
    ", ltp_case_timeout_secs()),\n        // The evaluator exposes one synthetic block-backed test device.  Make\n        // it visible to LTP's generic device-acquire helper so tests do not\n        // depend on a Linux loop-device stack that this kernel does not model.\n        ",
    ", ltp_env_shell_prefix(&env));\n            run_user_program_argv_in_timeout(\n                &run_dir,\n                &[&busybox_path, ",
    ", suite_dir.trim_start_matches('/')),\n    );\n    if matches!(fs::metadata(&helper_dir), Ok(meta) if meta.is_dir()) {\n        remove_dir_all(&helper_dir)?;\n    }\n    ensure_dir_all(&helper_dir)?;\n    let helper_busybox = ltp_helper_busybox_path(suite_dir, busybox_path);\n    for applet in LTP_BUSYBOX_APPLETS {\n        let wrapper_path = join_path(&helper_dir, applet);\n        let wrapper = format!(",
    ", suite_dir.trim_start_matches('/'), group),\n    );\n    if matches!(fs::metadata(&stage_root), Ok(meta) if meta.is_dir()) {\n        remove_dir_all(&stage_root)?;\n    }\n    ensure_dir_all(&stage_root)?;\n    copy_script_file(\n        &join_path(suite_dir, script_name),\n        &join_path(&stage_root, script_name),\n        busybox_path,\n        true,\n    )?;\n    Ok(stage_root)\n}\n\n#[cfg(all(feature = ",
    "./busybox",
    ".into(),\n        // Official OSKernel's glibc LTP judge counts the real LTP library\n        // status lines, but it matches their ANSI-colored form exactly.  Force\n        // LTP's own colorized output even though QEMU serial is captured as a\n        // pipe/file; this preserves genuine TPASS/TFAIL/TBROK/TCONF semantics\n        // and only changes their parseable presentation.\n        ",
    ".into(),\n    ];\n    if needs_case_resource_helper {\n        // Some LTP binaries are accompanied by per-case helper programs that\n        // ask the LTP framework to acquire a scratch filesystem device.  The\n        // evaluator exposes one synthetic block-backed test device but not a\n        // Linux loop-device stack, so declare the real supported filesystem\n        // type for every helper-backed case instead of naming individual LTP\n        // cases here.\n        env.push(",
    "/glibc",
    "`{busybox_path} {aplet}",
    "cyclictest",
    "exec {busybox_path} {applet} \\",
    "iozone",
    "iperf",
    "libcbench",
    "libctest",
    "lmbench",
    "netperf",
    "unixbench",
    "{indent}{busybox_path} {trimmed}"
  ]
}
