# LTP stable score improvement next run context

## Task statement
Continue improving `/root/oskernel2026-orays` LTP stable score using Ultragoal + Team. Start from the completed 75-case stable baseline and move toward 90-100 cases, targeted validation first.

## Desired outcome
- Promote only freshly validated LTP stable cases, preferably 80-88 first if 90-100 is risky.
- Preserve no fake PASS, no hardcoded case-name success, no silent SKIP of real failures.
- Timeout is counted separately and never as PASS.
- Use `scripts/ltp_summary.py` for internal TFAIL/TBROK/TCONF, timeout, ENOSYS, panic/trap; do not rely on `run-eval` exit code alone.
- Save this run's evidence under `docs/ltp-score-improvement-2026-05-24/`.

## Known baseline evidence read
- Prior `.omx/ultragoal/goals.json`: 19/19 complete; old Codex goal in this thread is clear (`get_goal` returned null on this run).
- `docs/ltp-score-improvement-2026-05-23/final-gate-quality-gate.json`: stable cases per libc/arch = 75, delta = +12, status PASS.
- Prior final gates: `cargo fmt --all -- --check`, `./run-eval.sh la`, `./run-eval.sh`, and both `scripts/ltp_summary.py` summary commands exited 0.
- LA/RV final LTP stable: PASS LTP CASE 150, FAIL LTP CASE 0, ltp-musl 75/0, ltp-glibc 75/0, TFAIL 0, TBROK 0, TCONF 4, LTP timeout 0, ENOSYS 0, panic/trap 0.
- `read02` is stable but pass_with_tconf and remains transparent; do not hide or reclassify it as clean.
- Full-output `timeout matches: 10` and `iperf-glibc ... end: fail` markers are non-LTP observations, not stable LTP promotion success.

## Current stable tail
`getpgid02`, `getsid02`, `getppid02`, `getuid03`, `geteuid02`, `getgid03`, `getegid02`, `getgroups03`, `uname02`, `wait01`, `wait02`, `getrlimit02` were the prior +12 additions.

## Candidate/risk starting set
Blocked/not-promoted set from the previous report:
`access02 access04 clock_getres01 clock_gettime01 dup03 fstatfs01 getpgid01 getsid01 kill02 link02 lseek02 mkdir02 nanosleep01 nanosleep02 pause01 pipe02 read02 rename01 rt_sigprocmask01 sigpending02 sigprocmask01 sigsuspend01 statfs01 statvfs01 sysinfo01 unlink05`.

Risk notes:
- `clock_getres01` may still be TCONF; do not promote as clean if so.
- `setsid()` is simplified; if `getsid01`/`getpgid01` require process-group-leader EPERM, implement real semantics before promotion.
- fs/statfs/sysinfo candidates need real ABI/errno semantics, not invented values or fake skips.
- time/signal candidates require fresh targeted validation and likely real signal/time behavior fixes.
- RV memory pressure and non-LTP benchmark markers are separate observation lanes unless they block targeted stable LTP gates.

## Likely touchpoints
- `examples/shell/src/cmd.rs` for `LTP_STABLE_CASES`, selectable batch, timeout-as-fail behavior.
- `scripts/ltp_summary.py` for matrix/category reporting.
- `examples/shell/src/uspace/process_abi.rs`, `process_lifecycle.rs`, `resource_sched.rs`, `synthetic_fs.rs`, `syscall_dispatch.rs` and related `fd_table.rs`, `metadata.rs`, `time_abi.rs`, `signal_abi.rs` if targeted failures require real ABI fixes.

## Team lane contract
Leader owns `.omx/ultragoal/goals.json` and `.omx/ultragoal/ledger.jsonl`; Team workers only report task status/evidence. Do not checkpoint Ultragoal from worker lanes.
