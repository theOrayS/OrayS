# LTP stable score improvement final gate report (2026-05-24)

## Result

Stable LTP batch increased from **75** to **85 cases / libc / arch**. The target 90-100 was not reached in this slice because the next candidate set had real TFAIL/TBROK/timeout/TCONF blockers; those cases were left out rather than hidden or converted to SKIP.

Promoted cases:

```text
getresuid01
getresuid02
getresuid03
getresgid01
getresgid02
getresgid03
getsid01
rt_sigaction02
sched_getscheduler01
uname04
```

Final local evaluator gate passed on both required architectures:

| Arch | PASS LTP CASE | FAIL LTP CASE | ltp-musl | ltp-glibc | Internal TFAIL/TBROK/TCONF | LTP timeout | ENOSYS | panic/trap |
| --- | ---: | ---: | --- | --- | --- | ---: | ---: | ---: |
| LA | 170 | 0 | 85/0 | 85/0 | {'TCONF': 4} | 0 | 0 | 0 |
| RV | 170 | 0 | 85/0 | 85/0 | {'TCONF': 4} | 0 | 0 | 0 |

`read02` remains transparent `pass_with_tconf` in both LA/RV and both libc variants:

- LA: la:glibc:read02, la:musl:read02
- RV: rv:glibc:read02, rv:musl:read02

The TCONF count is not hidden: it is 4 per arch total, all from `read02` (2 per libc). No timeout was counted as PASS.

## Files changed

- `.gitignore`
  - Added `!docs/ltp-score-improvement-2026-05-24/**/*.log` so this run's evidence logs under the report path are durable without surfacing older ignored logs.
  - Added `__pycache__/` and `*.pyc` to prevent Python bytecode artifacts from being tracked again.
- `examples/shell/src/cmd.rs`
  - Added 10 evidence-backed cases to `LTP_STABLE_CASES`.
- `examples/shell/src/uspace/linux_abi.rs`
  - Added `SIGSTOP_NUM` for unmaskable signal-mask semantics.
- `examples/shell/src/uspace/signal_abi.rs`
  - Added unmaskable signal handling, `rt_sigpending`, and `rt_sigsuspend` support; preserved/restored masks around sigsuspend delivery.
- `examples/shell/src/uspace/syscall_dispatch.rs`
  - Wired `rt_sigpending`, `rt_sigsuspend`, and `sysinfo` dispatch.
- `examples/shell/src/uspace/system_info.rs`
  - Added real `sysinfo` ABI fill/copy-out using allocator pages and live user thread count.
- `examples/shell/src/uspace/task_context.rs`
  - Added per-task sigsuspend restore-mask state.
- `scripts/ltp_summary.py`
  - Added/kept semantic wrapper normalization for official `FAIL LTP CASE <case> : 0` success lines.
  - Post-review hardened normalization so status code is source of truth: only `0` is PASS; every nonzero status is FAIL even if a future log token says PASS.
- `scripts/__pycache__/ltp_summary.cpython-310.pyc`
  - Removed tracked generated artifact.
- `docs/ltp-score-improvement-2026-05-24/`
  - Added context, Ultragoal/team evidence, targeted matrices, stable85 gates, final summaries, ai-slop report, code-review report, quality gate, and sync evidence.

Root `output_la.md` and `output_rv.md` were regenerated by the required final gate commands as local evaluator outputs. Durable raw copies are saved as `final-gate-output-la.md` and `final-gate-output-rv.md`; summaries are saved under `docs/ltp-score-improvement-2026-05-24/`.

## Modified functions/constants

See `changed-function-anchors.txt`; key symbols:

- `examples/shell/src/cmd.rs::LTP_STABLE_CASES`
- `examples/shell/src/uspace/linux_abi.rs::SIGSTOP_NUM`
- `examples/shell/src/uspace/signal_abi.rs::sys_rt_sigpending`
- `examples/shell/src/uspace/signal_abi.rs::sys_rt_sigsuspend`
- `examples/shell/src/uspace/task_context.rs::UserTaskExt::sigsuspend_restore_mask`
- `examples/shell/src/uspace/system_info.rs::sys_sysinfo`
- `examples/shell/src/uspace/syscall_dispatch.rs::dispatch_syscall`
- `scripts/ltp_summary.py::normalize_wrapper_status`

## Expected behavior changes

- Stable LTP autorun now runs 85 cases per libc instead of 75.
- Newly promoted getter/session/scheduler/signal/uname cases are run normally; no special PASS path was added.
- `rt_sigpending`, `rt_sigsuspend`, and `sysinfo` now have real syscall handlers instead of falling through as unsupported.
- `rt_sigprocmask` now keeps SIGKILL/SIGSTOP unmaskable.
- `ltp_summary.py` treats wrapper status code as authoritative and keeps timeout/internal TFAIL/TBROK/TCONF separate from wrapper PASS.

## Validation commands and exit codes

| Command | Evidence | Exit |
| --- | --- | ---: |
| `cargo fmt --all -- --check` | `final-cargo-fmt-check.status`, `post-review-cargo-fmt-check.status` | 0 |
| `./run-eval.sh la 2>&1 \| tee output_la.md` | `final-run-eval-la.status` | 0 |
| `./run-eval.sh 2>&1 \| tee output_rv.md` | `final-run-eval-rv.status` | 0 |
| `python3 -B scripts/ltp_summary.py output_la.md` | `final-ltp-summary-la.status` | 0 |
| `python3 -B scripts/ltp_summary.py output_rv.md` | `final-ltp-summary-rv.status` | 0 |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile scripts/ltp_summary.py` | `post-review-py-compile.status` | 0 |
| `git -C /root/oskernel2026-orays-remote diff --check` | `remote-sync-diff-check.status` | 0 |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile /root/oskernel2026-orays-remote/scripts/ltp_summary.py` | `remote-sync-py-compile.status` | 0 |

Targeted gates before final full gate:

- `targeted-promotion11-rv.status`: exit 0; 11-case promotion probe.
- `targeted-promotion11-la.status`: exit 0; 10 clean, `sched_getscheduler02` blocked.
- `stable85-targeted-la.status`: exit 0; LA stable85 170/0, TCONF 4, timeout 0.
- `stable85-targeted-rv.status`: exit 0; RV stable85 170/0, TCONF 4, timeout 0.

## Blocked / not promoted

| Case | Reason |
| --- | --- |
| `sched_getscheduler02` | LA musl targeted-promotion11 reports TFAIL=1/event-failures=1/status=FAIL |
| `getpgid01` | RV targeted-batch2 reports wrapper FAIL plus TFAIL/TBROK in both libc variants |
| `gettimeofday02` | RV musl targeted-batch2 timeout; timeout is not counted as PASS |
| `getgroups01` | RV musl targeted-batch2 TFAIL; only RV glibc was clean |
| `gettid02` | RV glibc targeted-batch2 wrapper FAIL/futex abort; RV musl clean only |
| `sched_getparam01` | RV targeted-batch2 TFAIL in both libc variants |
| `waitpid01` | RV targeted-batch2 TFAIL in both libc variants |
| `getrusage02` | RV targeted batches pass_with_tconf, not clean promotion |
| `clock_gettime03` | RV targeted-batch1 TBROK/fail_wrapper |
| `clock_gettime04` | RV targeted-batch1 TFAIL/fail_wrapper |
| `clock_nanosleep01` | RV targeted-batch1 timeout; timeout is not PASS |
| `clock_nanosleep02` | RV targeted-batch1 timeout/TFAIL; timeout is not PASS |
| `kill05` | RV targeted-batch1 TFAIL/TBROK/fail_wrapper |
| `sigaction02` | RV targeted-batch1 TFAIL/fail_wrapper |
| `sched_get_priority_max01` | RV targeted-batch1 fail_wrapper |
| `sched_get_priority_min01` | RV targeted-batch1 fail_wrapper |

Additional previously named FS/time/signal/sysinfo/statfs candidates remain blocked/not proven unless they have a fresh clean LA/RV × musl/glibc matrix. They were not promoted.

## Non-LTP observations

Full output still contains non-LTP `iperf ... end: fail` markers on both LA and RV for BASIC/PARALLEL/REVERSE UDP/TCP groups. These are outside the LTP stable promotion success condition and are not counted by `ltp_summary.py`, but they remain visible follow-up work.

## Remote sync

`/root/oskernel2026-orays-remote` exists as worktree branch `refactor/moss_kernel_like_remote`. This run synced only the touched source/report files plus the new 2026-05-24 docs into that worktree. Remote-only address-mapping files from commit `84e0a99b Support the remote LoongArch judge QEMU mapping` were not part of the sync scope and were preserved:

- `Cargo.lock`, `Cargo.toml`, `Makefile`
- `configs/platforms/axplat-loongarch64-qemu-virt.toml`
- `kernel/arch/axhal/build.rs`
- `scripts/make/platform.mk`
- `vendor/crates/axplat-loongarch64-qemu-virt/**`

Remote pre/post status is saved in `remote-sync-status-before.txt` and `remote-sync-status-after.txt`. Existing remote untracked files (`LoongArch输出.txt`, `Riscv输出.txt`, `docs/merge-moss-kernel-like-remote-2026-05-21/`) were not modified.

## Review / quality gate

- AI slop cleanup report: `final-gate-ai-slop-cleaner-report.md`
- Code review report: `final-gate-code-review-report.md`
- Recommendation: APPROVE
- Architectural status: CLEAR

## Syscall / errno / ABI-visible changes

Yes. This round includes ABI-visible syscall behavior changes in the shell/uspace layer:

- `rt_sigpending` and `rt_sigsuspend` now return real results instead of being unsupported.
- `sysinfo` now fills a Linux `struct sysinfo` and copies it to user memory.
- Signal masks now preserve SIGKILL/SIGSTOP as unmaskable.

No errno behavior was intentionally faked; unsupported or failing candidates remain blocked outside stable.

## Remaining risks and next batch suggestions

- Keep `read02` as transparent `pass_with_tconf` until the TCONF can be eliminated honestly.
- High-value next repairs: `sched_getscheduler02` LA musl TFAIL, `getpgid01` ESRCH/session semantics, `gettimeofday02` musl timeout, `getgroups01` musl semantics, `gettid02` glibc futex abort, `waitpid01`, `sched_getparam01`.
- Time/signal candidates (`clock_gettime03/04`, `clock_nanosleep01/02`, `kill05`, `sigaction02`) need real semantic fixes before promotion.
- Non-LTP iperf failures remain follow-up work and should not be conflated with stable LTP score.
