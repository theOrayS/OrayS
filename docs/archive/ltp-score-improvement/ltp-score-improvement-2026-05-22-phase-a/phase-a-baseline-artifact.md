# Phase A baseline artifact: stable85 -> stable100/110

Generated by OMX team `phase-a-20260522-stab-8231c807` worker-2 for task 2.

## Scope

This artifact is intentionally documentation-only. It edits only files under:

```text
docs/ltp-score-improvement-2026-05-22-phase-a/
```

No source, test harness, runner, or `.omx/ultragoal` state is changed by this worker task.

## Current source-of-truth baseline

The current checked-in stable batch has **85 cases / libc / arch** in `examples/shell/src/cmd.rs::LTP_STABLE_CASES`.

Last completed stable gate evidence is recorded in the phase-d artifacts referenced by this phase-a plan:

| Evidence | Result |
| --- | --- |
| Final stable count | 85 cases / libc / arch |
| LA stable LTP | `PASS LTP CASE 170`, `FAIL LTP CASE 0`, `ltp-musl 85/0`, `ltp-glibc 85/0` |
| RV stable LTP | `PASS LTP CASE 170`, `FAIL LTP CASE 0`, `ltp-musl 85/0`, `ltp-glibc 85/0` |
| Internal LTP signals | LA/RV both `TFAIL=0`, `TBROK=0`, `TCONF=4` |
| LTP timeout / ENOSYS / panic-trap | LA/RV all `0` |
| Known transparent caveat | `read02` remains `pass_with_tconf`; do not describe it as clean pass |
| Non-LTP caveat | `iperf ... end: fail` markers can remain outside the LTP stable promotion gate and must be reported separately |

## Cases promoted in the last completed slice

The last completed slice promoted exactly these 10 cases:

```text
getresuid01
getresuid02
getresuid03
getresgid01
getresgid02
getresgid03
getsid01
rt_sigaction02
sched_getscheduler01
uname04
```

## Phase A target and guardrails

Phase A should continue from stable85 toward **stable100** as the main target, with **stable105-110** as stretch only when fresh evidence is clean.

Mandatory guardrails for all follow-up workers:

- Do not fake PASS.
- Do not hardcode success by LTP case name.
- Do not silently turn real failures into SKIP.
- Do not count timeout as PASS.
- Do not rely on `run-eval` wrapper exit code alone; parse logs with `scripts/ltp_summary.py` and report internal `TFAIL/TBROK/TCONF`, timeout, ENOSYS, and panic/trap.
- Preserve targeted-batch -> promotion -> final-full-gate sequencing.
- Promotion requires LA/RV x musl/glibc evidence for each newly stable case.

## Recommended first wave

Start with a targeted Wave 1 around proc/sched/getter/wait cases, then promote only the clean subset:

```text
sched_getscheduler02
sched_getparam01
getpgid01
getgroups01
gettid02
waitpid01
waitpid02
waitpid03
gettimeofday02
getrusage02
getpriority01
getpriority02
setpriority01
setpriority02
times01
```

Known caveats before promotion:

- `sched_getscheduler02` had prior LA musl `TFAIL`; do not promote without a fresh clean LA/RV x musl/glibc matrix.
- `gettimeofday02` had prior timeout evidence; timeout must block promotion.
- `getrusage02` may be `pass_with_tconf`; do not count it as clean promotion unless TCONF is understood and explicitly accepted by the leader.
- `gettid02` had prior glibc futex abort evidence; verify real thread/futex/tid semantics before promotion.

## Recommended second wave

Prepare time/signal and FS/metadata cases in parallel, but keep promotion honest:

```text
clock_gettime03
clock_gettime04
clock_getres01
clock_nanosleep01
clock_nanosleep02
nanosleep01
nanosleep02
kill05
sigaction02
pause01
sigprocmask01
rt_sigprocmask01
sigpending02
sigsuspend01
access02
access04
link02
rename01
unlink05
mkdir02
lseek02
pipe02
dup03
statfs01
statvfs01
fstatfs01
sysinfo01
```

Promotion blockers to preserve rather than hide:

- `clock_getres01` TCONF is not clean success.
- `statfs01` / `statvfs01` / `fstatfs01` / `sysinfo01` require real ABI semantics; do not fabricate filesystem or memory information.
- Signal/time cases must not introduce long waits or timeout-based false positives.
- FS errno cases must fix real errno/path semantics instead of skipping negative paths.

## Artifact-path consistency note

Some historical phase-d artifacts in this checkout contain embedded references to `docs/ltp-score-improvement-2026-05-24/...` even though the phase-a handoff points workers at `docs/ltp-score-improvement-2026-05-21-phase-d/...`. This worker did not rewrite historical artifacts. Follow-up reports for the current run should use the current directory exactly:

```text
docs/ltp-score-improvement-2026-05-22-phase-a/
```

## Completion checklist for the leader

Before declaring Phase A complete:

- [ ] Save targeted raw logs, summaries, and promotion matrices in `docs/ltp-score-improvement-2026-05-22-phase-a/`.
- [ ] Show newly promoted case list and stable count after promotion.
- [ ] Run targeted LA/RV stable batch after promotion.
- [ ] Run final full gates only after targeted evidence is clean.
- [ ] Save `cargo fmt`, `run-eval`, and `ltp_summary.py` command exit evidence.
- [ ] Include blocked cases and exact blocker categories.
- [ ] Include final review/quality gate evidence.
