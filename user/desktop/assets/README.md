# Desktop assets

The current desktop ships no external bitmap, font, icon, or wallpaper files. Its visual assets are
generated by the Rust renderer at runtime. `manifest.json` is the fail-closed inventory used by
`scripts/desktop/check-assets.py`.
